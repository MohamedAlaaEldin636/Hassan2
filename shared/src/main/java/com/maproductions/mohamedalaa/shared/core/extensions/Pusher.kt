package com.maproductions.mohamedalaa.shared.core.extensions

/*
val options = PusherOptions()
        options.setCluster(PusherUtils.CHANNELS_CLUSTER)

        val pusher = Pusher(PusherUtils.CHANNELS_APP_KEY)
        pusher.connect()

        Timber.e("tracking order id ${viewModel.args.orderId}")

        val channel = pusher.subscribe(
            PusherUtils.getChannelNameTrackProviderLocation(viewModel.args.orderId)
        )

        channel.bind(
            PusherUtils.EVENT_NAME_TRACK_PROVIDER_LOCATION
        ) { event ->
            Timber.e("tracking event data ${event.data}")
        }
 */
